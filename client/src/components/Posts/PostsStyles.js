import { makeStyles } from "@material-ui/core";

export default makeStyles((main) => ({
  bigCon: {
    display: "flex",
    alignItems: "center",
  },
  tinyMargin: {
    margin: main.spacing(1),
  },
  activeDiv: {
    textAlign: "center",
  },
}));
